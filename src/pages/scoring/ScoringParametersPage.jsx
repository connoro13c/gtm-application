import { Box } from '@mui/material';
import { ScoringParameters } from '../../components/AccountScoring/ScoringParameters';

export const ScoringParametersPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <ScoringParameters />
    </Box>
  );
}; 